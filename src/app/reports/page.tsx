import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ReportsPage() {
  return (
    <PlaceholderPage
      title="Public review reports"
      description="A placeholder for evidence snapshots, rubric scores, risk badges, recommendations, fetch failures, and on-chain report certificates."
    />
  );
}
