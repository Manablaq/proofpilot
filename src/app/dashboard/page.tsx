import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function DashboardPage() {
  return (
    <PlaceholderPage
      title="Campaign dashboard"
      description="A minimal operator surface for future campaign creation, submission intake, review runs, re-checks, appeals, and human decisions."
    />
  );
}
